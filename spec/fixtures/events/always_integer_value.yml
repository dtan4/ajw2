event:
  id: event01
  realtime: false
  trigger:
    target: submitBtn
    type: onClick
    params:
    - id: count
      type: integer
      value:
        element: countTextBox
        func: getValue
        type: element
  action:
    type: always
    actions: []
ruby: |
  post "/event01" do
    content_type :json
    response = { _db_errors: {} }
    count = params[:count].to_i

    response.to_json
  end
