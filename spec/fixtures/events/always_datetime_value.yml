event:
  id: event01
  realtime: false
  trigger:
    target: submitBtn
    type: onClick
    params:
    - id: dt
      type: datetime
      value:
        element: dtTextBox
        func: getValue
        type: element
  action:
    type: always
    actions: []
ruby: |
  post "/event01" do
    content_type :json
    response = { _db_errors: {} }
    dt = Time.parse(params[:dt])

    response.to_json
  end
