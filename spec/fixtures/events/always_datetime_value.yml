event:
  trigger:
    id: event01
    target: submitBtn
    type: onClick
    realtime: false
    params:
    - id: dt
      type: datetime
      value:
        element: dtTextBox
        func: getValue
        type: element
  action:
    type: always
    actions: []
ruby: |
  post "/event01" do
    content_type :json
    response = { _db_errors: {} }
    dt = Time.parse(params[:dt])

    response.to_json
  end
