development:
<%= indent(@database.render_config(:development, @application), 1) %>

test:
<%= indent(@database.render_config(:test, @application), 1) %>

production:
<%= indent(@database.render_config(:production, @application), 1) %>
