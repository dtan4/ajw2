#!/usr/bin/env ruby

require "ajw2"

USAGE = <<-EOS
USAGE:
  $ ajw2 <source file> <output directory>
  $ ajw2 <source file> <output directory> <external resource directory>
EOS

unless (ARGV.length == 2) || (ARGV.length == 3)
  $stderr.puts USAGE
  exit 1
end

Ajw2::Cli.execute(ARGV)
